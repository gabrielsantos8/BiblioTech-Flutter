import 'package:flutter/material.dart';

class LivroDetalhe extends StatelessWidget {
  const LivroDetalhe({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}